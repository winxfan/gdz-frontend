Название проекта - гдз-по-фото.рф
Описание проекта - Пользователь делает фото задачи → загружает → сервис дает мгновенное решение → 5 бесплатных решений → далее подписка.
Валюта сервиса - молнии, по умолчанию пользователю дается 5 молний
Пользователь авторизуется по ip адресу, это нужно чтобы пользователь не смог сбросить лимиты валют обновлением браузера или другими махинациями
На сервер уходит запрос с post запросом где уходит поле ip адреса, сервер возвращает количество токенов
Тема по умолчанию - светлая



Под твой обновлённый стек (Next.js + Python + YC PostgreSQL + YC S3) я составлю **идеальную архитектуру**, **технологический пайплайн**, **модель данных**, **инфраструктуру**, и **конкретную логику для распознавания и решения задач**.

Ниже - полностью продуманная система, готовая к реализации.

---

### **Auth**

* Яндекс, Вконтакте, Mail.ru, 


# 🚀 **2. Архитектура проекта (идеальная схема)**

Вот схема взаимодействия:

```
Next.js (upload) → Python API → OCR → Parser → fal.ai solver → response
```

### Более подробно:

1️⃣ **Пользователь загружает фото**
Next.js API Route → отправка файла на Python API.

2️⃣ **Python backend**:

* сохраняет фото в S3
* делает OCR (извлекает текст)
* классифицирует предмет
* отправляет текст задачи в fal.ai
* получает решение

3️⃣ **Python** → сохраняет результат в PGSQL
4️⃣ **Frontend** получает JSON → показывает ответ

5️⃣ **Токены и лимиты** - считаются на Python backend (он надёжнее).

---


# 🌐 **7. Структура страниц (Next.js)**

### SEO-страницы:

* /
* /matematika
* /russian-kl
* /angliyskiy
* /5-klass
* /8-klass
  и т.д.

**Генерируем автоматически на основе семантики.**

---

# 📈 **8. Как будет выглядеть весь пайплайн**

### 1) Пользователь делает фото задачи

↓

### 2) Next.js → Python: `/solve-task`

↓

### 3) Python:

* upload image → S3
* OCR
* clean text
* detect subject/class
  ↓

### 4) fal.ai → решить

↓

### 5) Python → сохранить в PG

↓

### 6) Next.js → показать решение

↓

### 7) Списать токен

↓

### 8) Сохранить историю решений

---

# 💰 **9. Подписка + токены (идеальная схема)**

### Бесплатно:

* 5 молний

### Подписка:

* 100 молний (≈ 100 решений)
* цена 299–499 ₽/мес
* автопродление


будет два вида страниц
1. Корневая страница - для того чтобы пользователь мог изучить общую информацию о сайте
2. Специфичный страницы под поисковые запросы

url должен быть человеко понятный для поисковиков, на анлийском языке
будет два кластера url - предметы и классы
например /5-class или /mathematics
под эти страницу будет адаптирован контент, страницы перелинкованы

Структура корневой страницы
первый блок
заголовок  + описание (текст выравнен по середине, заголовок и описание на своих строках)
интерактивный блок изображение + UploadZoneWithChess (поделены на две половины, на мобильном устройстве - каждый компонент на своей строке)
третий блок - HowToUse
четвертый блок - InfoBlock 3 компонента подряд с чередованием картинки (слева и справа)
пятый блок - каталог по страницам разделенный на сегменты классы и предметы, там где классы - с 1 по 11 класс, там где предметы - перечислить все предметы в российских школах
шестой блок - FaqSection

Структура специфичной страницы
первый блок
заголовок  + описание (текст выравнен по середине, заголовок и описание на своих строках)
интерактивный блок изображение + UploadZoneWithChess (поделены на две половины, на мобильном устройстве - каждый компонент на своей строке)
третий блок - HowToUse
четвертый блок - InfoBlock 3 компонента подряд с чередованием картинки (слева и справа)
пятый блок - FaqSection

структура Header - логотип (надпись гдз-по-фото.рф), элементы меню (Математика, Русский язык, Физика и так далее), Кнопка быстрый вход по нажатию открывает AuthDialog